name: buildModule
on:
    workflow_call:
        inputs:
            moduleName:
                required: true
                type: string

jobs:
    buildModule:
        name: Build ${{ inputs.moduleName }}
        runs-on: ubuntu-latest
        permissions:
            contents: read
            security-events: write
            actions: read
        steps:
            - name: Checkout code
              uses: actions/checkout@v4

            # - name: Setup pnpm environment
            #   uses: ./.github/actions/setup-pnpm
