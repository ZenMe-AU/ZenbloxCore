name: deployModuleProd
on:
    workflow_call:
        inputs:
            moduleName:
                required: true
                type: string

jobs:
    deployModuleProd:
        name: Run deployModuleProd
        runs-on: ubuntu-latest
        permissions:
            contents: read
            security-events: write
            actions: read
        steps:
            - name: Checkout code
              uses: actions/checkout@v4
            # - name: Setup pnpm environment
            #   uses: ./.github/actions/setup-pnpm
            - name: Print which module is building
              run: echo "Building module${{ inputs.moduleName }} deployModuleProd"
