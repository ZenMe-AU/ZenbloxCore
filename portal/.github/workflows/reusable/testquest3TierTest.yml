name: testquest3TierTest

on:
    workflow_call:
jobs:
    testquest3TierTest:
        name: Run testquest3TierTest
        runs-on: ubuntu-latest
        permissions:
            contents: read
            security-events: write
            actions: read
        steps:
            - name: Checkout code
              uses: actions/checkout@v4

            # - name: Setup pnpm environment
            #   uses: ./.github/actions/setup-pnpm
            - name: print input
              run: echo "testquest3TierTest"
