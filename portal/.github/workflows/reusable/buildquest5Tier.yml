name: buildquest5Tier
on:
    workflow_call:
jobs:
    buildquest5Tier:
        name: Run buildquest5Tier
        runs-on: ubuntu-latest
        permissions:
            contents: read
            security-events: write
            actions: read
        steps:
            - name: Checkout code
              uses: actions/checkout@v4
            # - name: Setup pnpm environment
            #   uses: ./.github/actions/setup-pnpm
            - name: print input
              run: echo "buildquest5Tier"
